# 로컬 `npm run dev` (frontend 디렉터리 기준)
# 이 파일을 .env.local 로 복사한 뒤 값을 채우세요. (.env.local 은 git 에 포함되지 않습니다.)
NEXT_PUBLIC_API_URL=http://localhost:8000
# 백엔드 API_ADMIN_TOKEN 과 동일한 값 (관리자 로그인 시 클라이언트에서 입력값과 비교하는 힌트로도 사용)
NEXT_PUBLIC_API_ADMIN_TOKEN=
